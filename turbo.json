{
	"$schema": "https://turbo.build/schema.json",
	"pipeline": {
		"build": {
			"dependsOn": ["^build"],
			"outputs": ["built/"]
		},
		"build:type": {
			"dependsOn": ["^build:type"],
			"outputs": ["types/"]
		},
		"lint": {},
		"test": {},
		"test-and-coverage": {}
	}
}
